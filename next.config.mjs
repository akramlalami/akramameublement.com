
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            '127.0.0.1',
            'akramapi.akramameublement.com' 
           
        ],
    },
    output: 'standalone',
};

export default nextConfig;